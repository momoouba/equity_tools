# 上市进展联调验收记录（2026-04-14 ~ 2026-04-22）

## 1. 本次目标

- 完成四类同步能力从“服务骨架”到“可运行链路”的落地与联调：
  - 打新日历
  - 辅导备案（写入 `ipo_progress`）
  - 境外备案审核
- 打通“手动同步 -> 执行日志查询”的闭环。

---

## 2. 关键实现结论

### 2.1 同步能力

- 打新日历：已接入 AkShare 抓取并入库 `ipo_new_share`，具备判重更新统计。
- 打新日历：已并入上市首日指标补齐（`first_day_close`、`first_day_chg_pct`、`first_day_market_cap`、`total_issued_shares`），对空值支持每日更新。
- 辅导备案：已接入 CSV/页面抓取链路并写入 `ipo_progress`，`register_address` 对应“派出机构”。
- 境外备案审核：已接入 Excel/CSV 解析链路，**写入 `ipo_progress`**（`board=境外发行备案`）；独立表 `ipo_overseas_filing` 已废弃并由迁移脚本 DROP。

### 2.2 任务治理能力

- 已接入并发互斥（同源同窗口）、指数退避重试（最多 5 次）、执行日志落盘。
- 新增执行日志表：`listing_sync_execution_log`。
- 新增日志查询接口：`GET /api/listing/listing-sync-execution-log`。

### 2.3 手动同步日志闭环

- 四个业务同步接口均已接入 `listing_sync_execution_log`。
- 日志可记录：`source_type`、`task_key`、`inserted/updated/skipped`、`status`、`error_message`、执行时间区间。

---

## 3. 环境与数据源配置

当前最小可用配置已写入 `.env`：

- `CSRC_GUIDANCE_CSV_PATH=E:/桌面/equity_news/news/server/utils/上市进展/mock-guidance.csv`
- `OVERSEAS_FILING_FILE_PATH=E:/桌面/equity_news/news/server/utils/上市进展/mock-overseas.csv`

配套样例文件：

- `server/utils/上市进展/mock-guidance.csv`
- `server/utils/上市进展/mock-overseas.csv`

---

## 4. 联调结果（HTTP）

联调接口（区间：`2026-04-01` ~ `2026-04-30`）：

- `POST /api/listing/new-share/sync`：成功，`updated=13`
- `POST /api/listing/guidance-progress/sync`：成功
- `POST /api/listing/overseas-filing/sync`：成功
- `GET /api/listing/listing-sync-execution-log?page=1&pageSize=20`：成功，当前已可看到三类 `source_type` 日志记录

### 4.1 港股打新新增需求联调（2026-04-14）

- 变更后重启服务并执行：
  - `POST /api/listing/new-share/sync`（`from=2026-04-01,to=2026-04-30`）
- 实测结果：
  - `new-share/sync` 返回 `fetched=18, inserted=5, updated=13`，说明港交所“新上市”记录已进入打新主链路；
  - `listing-sync-execution-log` 可见本次 `new_share` 记录，`inserted_count=5`、`updated_count=13`。

---

## 5. 已知风险与说明

- 首日指标依赖外部行情源，可能出现网络抖动（`RemoteDisconnected`）导致单票当日未补齐。
- 当前会在后续同步中继续补抓空值，并写日志 `error_message` 便于定位。

---

## 6. 建议上线前检查

- 确认生产环境 CSV/URL 数据源配置可访问。
- 按真实日期区间跑一轮三类手动同步并检查日志页可见。
- 重点检查打新日历首日指标空值补齐效果（收盘价、涨幅、市值、中签率）。

---

## 7. 【证监会政府信息公开·备案通知书列表抓取 · 新增 · 2026-04-20】联调与验收（待执行）

**对应需求**：`上市进展需求.md` **14.5、15.8.1、16.3**；**接口文档** §5 补充说明。

### 7.1 验收清单（开发完成后勾选）

- [ ] **列表+详情**：入口 `channelid=8f3f0d4be56b4f8aa8183b3234b88ede` 列表仅处理标题含 **备案通知书**；详情页解析 **文号、发文日期、企业全称、`一、`段交易所**。
- [ ] **入库字段**：`board=境外发行备案`，`status=备案完成`，`project_name` **无简称时用企业全称**，`register_address`=**文号**，`exchange` 经 **纳斯达克/纽交所/新交所/港交所** 等映射；`receive_date`/`f_update_time` 与 **15.8** 现网一致。
- [ ] **合并**：与 **Excel 同日同企业** 行合并为一条，`register_address` **以文号覆盖**；企业匹配 **规范化全等**。
- [ ] **判重**：`board` + `project_name` + **文号**（存于 `register_address`）。
- [ ] **调度**：与 **Excel 境外备案** 定时 **同一执行时刻/同一任务**；日志可区分 `excel` / `notice_html`（或等价字段）。
- [ ] **手动同步**：`POST /api/listing/overseas-filing/sync`（或最终实现路径）**`from`~`to` 跨度 >30 天** 时拒绝并提示。
- [ ] **重试**：单次失败 **最多 5 次**，**仅失败时重试**。
- [ ] **增量**：以 **发文日期** 为主水位。

### 7.2 联调建议步骤（实现就绪后）

1. 配置可访问的证监会列表 URL（或测试镜像页，若有）。
2. 选手动区间 **≤30 天**，执行境外备案同步，检查 `ipo_progress` 新增/更新及 **合并** 场景。
3. `GET /api/listing/listing-sync-execution-log` 核对来源、重试、错误摘要。

---

## 8. 【打新日历企业全称（中英）AI补齐 · 新增-2026-04-22】联调与验收（待执行）

**对应需求**：`上市进展需求.md` **14.6、15.5.1、17.24**；**接口文档** §3、§8 补充说明。

### 8.1 验收清单（开发完成后勾选）

- [ ] **表结构**：`ipo_new_share` 新增 `enterprise_full_name_cn`、`enterprise_full_name_en`、`enterprise_full_name_display` 字段，字段注释齐全。
- [ ] **触发时机**：每日打新同步完成后，仅对全称展示值为空的数据触发补齐任务。
- [ ] **AI 入参**：请求模型时仅携带 `stock_code`、`stock_name`（以及系统提示词）。
- [ ] **AI 出参**：能稳定解析并写入中英全称；`enterprise_full_name_display` 按“中文 / 英文”规则拼接。
- [ ] **模型来源**：调用“系统配置 -> AI模型配置管理”的生效模型，不写死模型参数。
- [ ] **提示词来源**：优先“模型提示词设置”配置；缺失或禁用时走代码兜底提示词。
- [ ] **列表展示**：打新日历新增“企业全称（中/英）”列，单行不换行，超出省略，鼠标悬停展示完整内容。
- [ ] **邮件不变**：上市进展邮件模板与顺序、字段保持不变，不因本需求新增或调整。

### 8.2 联调建议步骤（实现就绪后）

1. 手动执行 `POST /api/listing/new-share/sync`（选取有空全称样本区间）。
2. 查询 `GET /api/listing/new-share`，核对中英字段与展示字段是否回填。
3. 在页面确认“企业全称（中/英）”列样式（单行/省略/浮显）。
4. 触发一次上市进展邮件试发，核对邮件内容未发生变更。

