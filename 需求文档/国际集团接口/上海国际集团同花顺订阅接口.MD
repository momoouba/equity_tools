上海国际集团同花顺订阅接口
接口基础信息
接口说明：上海国际企业数据总线系统的同花顺订阅接口实现了通过标准接口查询上海国际集团外部融合平台同花顺订阅相关信息数据的功能，同步同花顺渠道下的订阅推送、持仓关注、资讯订阅等相关数据。
接口地址（测试环境 - 内网）：http://61.152.159.102:21313/esb/gateway/arsenal/yq_qdc/industry_chain_api/v1/client/sync/org/add  
请求方式：POST正文格式：application/json; charset=UTF-8
参数名	示例	类型	是否必填	说明
X-App-Id	p8m5VkfX	String	是	Ipass 平台授权的消费方标识，固定值
X-Sequence-No	F9efb7A4-2a94-fD05-D9E7-DeAE67283fa4	String	是	消息流水号：自定义 uuid
X-Timestamp	1741089263217	String	是	时间戳
APIkey	vFhh9x0C7ihr78dx	String	是	消费方认证，固定值
请求报文格式
json{
 "creditcode":"91120222MABNWFGC3E"
}

属性	名称	数据类型	是否必填	说明
creditcode	统一社会系统代码	String	是	最多传100个统一社会信用代码，传多个时中间以“,”进行分割。例如（“91440300MA5H7LC70E,91440300MA5HJA0U8L”）

接口调用与日期补拉规则
1. 接口由定时任务或任务调度系统按天触发，**正常情况**：当天 \(T\) 日调用时，`query_date` 取前一日 \(T-1\) 的日期。
2. 系统需要记录**上一次成功调用时实际使用的 `query_date`**（记为 `last_query_date`），而不是仅记录调用时间。
3. 当再次触发接口时，应从 `last_query_date` 的下一天开始，直到本次调用日的前一日，**逐日补拉**，每个自然日调用一次接口：
   - 例如：上一次请求时间为 2026-02-27，使用的 `query_date` 为 2026-02-26，今天为 2026-03-02，
   - 则本次触发时需要调用 3 次接口，`query_date` 分别为：2026-02-27、2026-02-28、2026-03-01。
4. **节假日场景**：节假日期间不会触发调用，节后第一次触发时，需要按照上述规则，
   - 从节前最后一次查询的 `query_date` 的下一天开始，
   - 一直到节后首次触发日的前一日为止，
   - 对中间所有自然日执行补拉，确保节假日前一天及节假日期间的数据不会遗漏。



响应报文样例json{
    "status_code": 0,
    "status_msg": "请求成功",
    "data": {
        "total": 1,
        "sync_num": 1,
        "un_sync_name": []
    }
}


响应字段说明：

参数名称	类型	参数中文名	说明
status_code	integer	状态码	
status_msg	string	状态信息	
data	object	数据	
data>> total	string	输入公司的总数	
data>> un_sync_name	array	匹配失败的公司名称/统一社会信用代码列表	
data>> sync_num	int	匹配成功的数量（同步数量）	

接口定时触发时逻辑：
当定时触发接口时，查询company表
SELECT unified_credit_code FROM company where updated_at = CURRENT_DATE
然后将参数按照请求的样式，触发该接口

接口手动触发逻辑：
当手动触发接口时，弹窗日期假设为start_date 和end_date
SELECT unified_credit_code FROM company where updated_at <= end_date and updated_at >= start_date
然后将参数按照请求的样式，触发该接口