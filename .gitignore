# Windows reserved-name artifact from accidental shell redirects
news/server/routes/performance/nul
**/nul

# Local news snapshots — commit only on branch `backup` (see docs/agents/backup-branch.md)
news-backup-*/

# Visual Studio workspace (local IDE state)
.vs/

# Local agent / Cursor skill installs（release/clean 不入库；已跟踪的 smart-git-push 不受影响）
.agents/
.cursor/rules/
.cursor/skills/darwin-skill/
skills-lock.json

# 上市进展爬虫本地探针产物（cookie/HTML/临时脚本，勿提交）
news/server/scripts/bse.cookie
news/server/scripts/bse*.html
news/server/scripts/bse_detail.js
news/server/scripts/bse_detail_api_*.json
news/server/scripts/probeBse*.js

# One-off golden-set export analysis (local only)
scripts/analyze_golden_export.py
scripts/golden_export_analysis.json

# 根目录需求文档：正式入库在 sync-issue-5849a；release/clean 仅本地保留副本、不提交
# （切分支后由 .git/hooks/post-checkout 或 scripts/restore-req-docs.ps1 从 sync 恢复）
/需求文档/

# SSL private keys and certs (deploy from server secrets, not git)
**/ssl/*.pem
news/client/src-backup-old/
news/client/**/*.backup
scripts/competitor-embedding-poc/
